# Current Feature

## Status

Not Started

## Goals

## Notes

## History

<!-- Keep this updated. Earliest to latest -->

- Initial setup of Next.js
- Project setup and boilerplate cleanup
- Dashboard UI Phase 1
- Dashboard UI Phase 2
- Dashboard UI Phase 3
- Prisma + Neon PostgreSQL Setup
- Seed Data
- Dashboard Collections
- Dashboard Items
- Stats & Sidebar
- Add Pro Badge to Sidebar
- Code Quality Quick Wins (Code Scan Cleanup): explicit TypeIcon icon map, zod/vitest devDependencies, root redirect to /dashboard
- Auth Setup - NextAuth + GitHub Provider (Phase 1): split auth.config.ts/auth.ts for edge compatibility, PrismaAdapter with JWT strategy, GitHub OAuth, /dashboard/* route protection via proxy.ts, Session.user.id type augmentation
- Auth Credentials - Email/Password Provider (Phase 2): Credentials provider placeholder in auth.config.ts overridden with real bcrypt + Prisma validation in auth.ts, zod schemas (signInSchema/registerSchema) in src/lib/validations/auth.ts, registration API route at /api/auth/register
- Auth UI - Sign In, Register & Sign Out (Phase 3): custom /sign-in and /register pages replacing NextAuth defaults (pages.signIn, proxy callbackUrl preserved), signInWithCredentials/signInWithGitHub/signOutAction server actions, sidebar UserMenu/UserAvatar reflecting real session (avatar with initials fallback, name, email), dropdown-menu and toast base-ui primitives added, minimal /profile page, registration success toast
- Email Verification on Register: Resend integration (src/lib/resend.ts), verification tokens reusing the Auth.js VerificationToken model (src/lib/auth/verification-token.ts), send-on-register (src/app/api/auth/register/route.ts), /verify-email page handling the link click, Credentials authorize() blocks unverified sign-in via custom EmailNotVerifiedError (src/lib/auth/errors.ts), resendVerificationEmail server action wired into SignInForm and a new ResendVerificationButton
- Email Verification Toggle: EMAIL_VERIFICATION_ENABLED env flag (src/lib/auth/email-verification.ts, default enabled, "false" disables) — register route skips token/email and marks users verified immediately, auth.ts authorize() skips the unverified check, RegisterForm and /verify-email route around it so no UI dead-ends when disabled
- Forgot Password: /forgot-password and /reset-password pages, requestPasswordReset/resetPassword server actions (src/actions/auth.ts), password reset tokens reuse the VerificationToken model via a namespaced identifier (src/lib/auth/password-reset-token.ts) to avoid colliding with email-verification tokens, sendPasswordResetEmail via Resend (src/lib/email/send-password-reset-email.ts), 1-hour token TTL, GitHub-only accounts silently skipped (no passwordHash), "Forgot password?" link added to SignInForm, also fixed a pre-existing Base UI nativeButton warning on Button+Link usages across auth pages
- Profile Page: expanded /profile with real per-user data (src/lib/db/profile.ts using session.user.id instead of the demo-user pattern) — member-since date, usage stats and item-type breakdown (ProfileStats.tsx), ChangePasswordDialog (email/password accounts only, gated on passwordHash) and DeleteAccountDialog (type-to-confirm, cascading delete) via src/actions/profile.ts, changePasswordSchema added to src/lib/validations/auth.ts
- Rate Limiting for Auth: Upstash Redis + @upstash/ratelimit sliding-window utility (src/lib/rate-limit.ts, fails open if Upstash unconfigured/unavailable), applied to register (API route, 429 + Retry-After header) and to login/forgot-password/reset-password/resend-verification (Server Actions in src/actions/auth.ts, surfaced via existing success/error result pattern since Server Actions can't set HTTP status codes), fixed a pre-existing bug where SignInForm/ResendVerificationButton always showed "email sent" regardless of the action result
- Items List View: dynamic route /items/[type] (src/app/(app)/items/[type]/page.tsx) fetching items filtered by type via new getItemTypeByName/getItemsByType (src/lib/db/items.ts), responsive 2-col ItemCard grid (src/components/dashboard/ItemCard.tsx) reusing ItemRow's border-color+TypeIcon pattern, plural-to-singular slug mapping (src/lib/item-type-slug.ts), moved dashboard layout/page into a new (app) route group so /items/[type] shares the sidebar/topbar (no URL changes), fixed pre-existing Sidebar.tsx/ItemRow.tsx links that pointed at singular type names, added /items/* to proxy.ts's protected matcher
- Vitest Setup: vitest.config.ts (node environment, @/* alias, src/**/*.test.ts only so component tests aren't picked up), test/test:watch scripts in package.json, example unit test for src/lib/item-type-slug.ts as a pattern for future server-action/utility tests
- Three-Column Items Grid: added `lg:grid-cols-3` breakpoint to the items grid (src/app/(app)/items/[type]/page.tsx), keeping the existing 1/2-column responsive breakpoints below it; no ItemCard changes needed since it's already width-fluid
- Item Drawer: right-side shadcn Sheet opens on item click showing full detail (content, language, tags, collections, created/updated) fetched via new auth-checked `/api/items/[id]` route calling `getItemById` (src/lib/db/items.ts); `ItemDrawerProvider` context (src/components/dashboard/item-drawer-context.tsx) shared app-wide via the `(app)` layout so both the dashboard and `/items/[type]` pages open the same drawer instance; `ItemCard`/`ItemRow` converted from `<Link>` (to a `/items/[type]/[id]` route that never had a page) to buttons that open the drawer instead; action bar (Favorite/Pin/Copy/Edit/Delete) renders per the screenshot but only Copy is wired (clipboard) — Favorite/Pin/Edit/Delete have no mutations yet; unit tests for `getItemById` in src/lib/db/items.test.ts
- Item Drawer Edit Mode: Edit button toggles the drawer into inline edit mode (Save/Cancel replace the action bar) via new `updateItem(itemId, data)` server action (src/actions/items.ts) — Zod-validated (`updateItemSchema` in src/lib/validations/items.ts), `auth()`-checked, ownership-checked — backed by a new `updateItem` query function (src/lib/db/items.ts, tags: disconnect-all + connect-or-create, returns updated `ItemDetail`); editable fields are Title/Description/Tags for all types plus Content/Language/URL shown conditionally per item type; new `Textarea` UI primitive (src/components/ui/textarea.tsx, didn't exist before); Save shows a toast and calls `router.refresh()`; unit tests for `updateItem` in src/lib/db/items.test.ts (no test added for the action itself, matching the existing untested-action convention from auth.ts/profile.ts)
- Item Delete: Delete button in the item drawer's action bar now opens a confirmation `Dialog` (reusing the Base UI-backed primitive from `DeleteAccountDialog.tsx`, no separate AlertDialog added) before deleting via new `deleteItem(itemId)` server action (src/actions/items.ts) — `auth()`-checked, ownership-checked — backed by a new `deleteItem` query function (src/lib/db/items.ts, Prisma `item.delete` scoped by `userId`); on success the dialog and drawer close, a toast confirms, and `router.refresh()` updates the list; R2 file/image cleanup on delete deliberately out of scope; unit tests for `deleteItem` in src/lib/db/items.test.ts
- Item Create: "New Item" button in the top bar now opens a `CreateItemDialog` (src/components/dashboard/CreateItemDialog.tsx) with a type-selector button row (snippet/prompt/command/note/link, file/image excluded as out of spec) and fields shown conditionally per type (content for snippet/prompt/command/note, language for snippet/command, required URL for link) plus title/description/tags for all types; new `createItem` server action (src/actions/items.ts) — Zod-validated (`createItemSchema` in src/lib/validations/items.ts, enforces URL presence for link via `.refine`) and auth-checked — backed by a new `createItem` query function (src/lib/db/items.ts, resolves the type via the existing `getItemTypeByName` so users can't create items under a type they don't own, sets `contentType` to `URL` for links and `TEXT` otherwise); on success shows a toast, closes the modal, and calls `router.refresh()`; `itemTypes` threaded from the `(app)` layout's existing `getSystemItemTypes` fetch down through `TopBar` into the dialog; unit tests for `createItem` in src/lib/db/items.test.ts. Known issue (parked, not yet root-caused): after creating an item, opening any item drawer afterward in the same session shrinks the window/adds scroll, every time, regardless of window width — suspected to be Base UI's Dialog scroll-lock (a global singleton that adjusts `<body>`/`<html>` inline styles while a dialog is open) leaving stale styles after the create dialog closes that every later dialog/drawer inherits; needs live devtools inspection of `<body>`'s inline `style` attribute when it happens to confirm before fixing.
- Code Editor (Monaco): new `CodeEditor` component (src/components/dashboard/CodeEditor.tsx) wrapping `@monaco-editor/react` with a fixed dark theme (`synapse-dark`, defined via `monaco.editor.defineTheme` with themed scrollbar-slider colors), macOS-style window dots, an in-header copy button + language label, fluid height capped at 400px via `editor.getContentHeight()`, and a `readOnly` prop for display vs. edit modes; wired into `ItemDrawer.tsx` (both the readonly display section and the edit-mode form) and `CreateItemDialog.tsx`'s content field, but only for snippet/command items (`LANGUAGE_TYPES`) — note/prompt keep the plain `Textarea`/`<pre>`.
- Type-Specific Add Button: `/items/[type]` page (src/app/(app)/items/[type]/page.tsx) now also fetches `itemTypes` and renders a type-specific "Add {type}" button next to the page title (hidden for file/image, which aren't creatable yet) that opens `CreateItemDialog` preselected to that type via a new `defaultTypeName` prop, plus a new `trigger` prop so the page can supply its own button instead of the generic "New item"; `EXCLUDED_TYPES` is duplicated locally in page.tsx rather than imported from `CreateItemDialog` since importing a plain value export from a `"use client"` module into a Server Component wraps it in a client-reference proxy and breaks array methods like `.includes`. `ItemCard.tsx`'s description paragraph now always renders (with `min-h-10`) instead of being conditionally omitted, so grid cards are a uniform height whether or not an item has a description (bundled into this branch per user request, not a separate spec). Known issues (parked, not yet root-caused): (1) the type preselection was reported to still fall back to the first item type in some cases even after moving the reset into `handleOpenChange` in `CreateItemDialog.tsx`; (2) the global "New item" button's Content field was reported to always render the code editor regardless of the selected type — the gating logic (`LANGUAGE_TYPES.includes(typeName)`) reads correctly on inspection, so this needs live repro to confirm before fixing.
- Markdown Editor: new `MarkdownEditor` component (src/components/dashboard/MarkdownEditor.tsx) with a tabbed Write/Preview interface using `react-markdown` + `remark-gfm` for GFM rendering, an in-header copy button matching `CodeEditor`'s style, fluid height capped at 400px (auto-growing textarea in Write mode), and a `readOnly` prop that forces Preview-only display; replaces `Textarea` for `note`/`prompt` content only (new `MARKDOWN_TYPES` constant, duplicated in both files per the existing `CONTENT_TYPES`/`LANGUAGE_TYPES` convention) in `CreateItemDialog.tsx` and `ItemDrawer.tsx` (both edit mode and readonly view) — snippets/commands keep `CodeEditor` unchanged; `.markdown-preview` CSS class added to globals.css for headings/code/lists/blockquotes/links/tables styling, verified to correctly override Tailwind v4 preflight resets via selector specificity. Known discrepancy (not yet resolved): the spec's stated colors (`bg-[#1e1e1e]` container / `bg-[#2d2d2d]` header) don't actually match `CodeEditor`'s real colors (`bg-[#18181b]` throughout), so `MarkdownEditor` and `CodeEditor` look very slightly different in dark mode — implemented per the literal spec, flagged for the user to decide whether to align them.
- File & Image Upload (Cloudflare R2): file/image system types wired into `CreateItemDialog.tsx` (previously excluded) via a new `FileUpload` component (src/components/dashboard/FileUpload.tsx — drag-and-drop, upload progress bar, image thumbnail/file-info preview) backed by a new auth-checked `/api/upload` route and `src/lib/r2.ts` (S3-compatible client for Cloudflare R2, `@aws-sdk/client-s3`); shared size/extension/MIME constraints in `src/lib/upload-constraints.ts` drive both client validation and server enforcement; new auth+ownership-checked `/api/items/[id]/download` route proxies the file through the server (avoids CORS, sets `Content-Disposition`) with a Download link + image preview/file-info card added to `ItemDrawer.tsx`; `deleteItem` (src/lib/db/items.ts) now returns the deleted item's `fileUrl` so the `deleteItem` action (src/actions/items.ts) can clean up the R2 object after a successful DB delete, closing the gap left by the earlier Item Delete feature; `createItemSchema` (src/lib/validations/items.ts) validates that `fileUrl` is actually hosted under `R2_PUBLIC_URL` (via `isR2Url`) since Server Actions are callable independent of the UI and an unvalidated URL would let `/api/items/[id]/download`'s server-side `fetch` be used as an SSRF proxy; unit tests added for `upload-constraints.ts` and `createItemSchema` (including a regression test for the SSRF guard) in addition to the existing `items.test.ts` coverage. Bundled fixes discovered while testing, all shared/root-caused rather than one-off patches: (1) `DialogContent` (src/components/ui/dialog.tsx) is `display:grid` without `minmax(0,...)` column sizing, so an unbreakable long string (e.g. a filename) forced the grid track wider than the dialog, overflowing horizontally — fixed with `grid-cols-1`, which also fixed the same class of bug in `ItemDrawer.tsx`'s title header (added `min-w-0 flex-1 truncate`) since any item type can have a long unbroken title, not just files; (2) the "New item" dialog scrolled inside a cramped `max-h-[85vh]` box — `Dialog`/`Sheet` (dialog.tsx, sheet.tsx) now use Base UI's `Dialog.Viewport` as the scrollable positioning container with two `flex-1` spacers around the popup (centers short dialogs vertically, shrinks to 0 instead of compressing the popup when content is taller than the screen, so the whole overlay scrolls instead of an inner box); (3) a hydration mismatch on every dialog's trigger/close buttons — `DialogTrigger`/`DialogClose`/`SheetTrigger`/`SheetClose` no longer set an explicit `data-slot` (including the built-in close "X" inside `DialogContent`/`SheetContent`), since when composed with `render={<Button/>}` (which sets its own `data-slot`), Base UI's render-prop merge resolved the winning value differently between SSR and hydration.
- Image Gallery View: new `ImageCard` component (src/components/dashboard/ImageCard.tsx) replaces `ItemCard` for the image type only on `/items/[type]` — 16:9 `aspect-video` thumbnail with `object-cover` (crops non-16:9 sources), a 5%/300ms hover zoom (`scale-105`, `duration-300`), and a fallback icon for a missing `fileUrl`; swapped in conditionally in page.tsx (`typeName === "image"`) reusing the existing 1/2/3-column responsive grid rather than introducing a separate gallery layout; `ItemWithType` (src/lib/db/items.ts) gained `fileUrl`/`fileName` fields (Prisma already returned them, only the type was missing them) so `getPinnedItems`/`getRecentItems`/`getItemsByType` all now expose them. Browser-verified with Playwright (upload/render/hover/delete across square, wide, and exact-16:9 test images, plus mobile single-column). Scoped deliberately to the `/items/[type]` grid per spec — the dashboard's pinned/recent sections still render images as generic `ItemRow` list rows, not thumbnails, since that wasn't in the spec's stated scope.
- File List View: new `FileListRow` component (src/components/dashboard/FileListRow.tsx) replaces `ItemCard` for the `file` type only on `/items/[type]` — single-column list (divide-y bordered container in page.tsx) with an extension-based icon (`.pdf/.txt/.md`→FileText, `.json`→FileJson, `.yaml/.yml/.xml/.toml/.ini`→FileCode, `.csv`→FileSpreadsheet, fallback→File), file name, size + upload date (stacks vertically below `sm:`), hover highlight, and a Download button reusing the existing `/api/items/[id]/download` route with `stopPropagation` so it doesn't also open the drawer; row itself is a `div[role=button]` (not a `<button>`) since it needs to contain a real nested `<a>` for the download link — invalid to nest interactive elements inside a native `<button>`. `ItemWithType` (src/lib/db/items.ts) gained `createdAt`/`fileSize` fields (Prisma already returned them, only the type was missing them, same pattern as the earlier `fileUrl`/`fileName` addition). `image` type untouched, still uses `ImageCard`.
- ItemDrawer Refactor (Code Scan Cleanup): split the 522-line `ItemDrawer.tsx` into `ItemDrawerActions` (action bar), `ItemDrawerEditForm` (edit-mode fields, exports `EditState`/`toEditState`), `ItemDrawerView` (read-only display), and `DeleteItemDialog` (confirmation dialog) — `ItemDrawer.tsx` is now just the data-fetching/state shell; extracted the CodeEditor/MarkdownEditor/Textarea chooser (previously duplicated verbatim in `ItemDrawer.tsx` and `CreateItemDialog.tsx`) into a shared `ItemContentField` component backed by one `src/lib/item-type-kinds.ts` (`CONTENT_TYPES`/`LANGUAGE_TYPES`/`MARKDOWN_TYPES`/`FILE_TYPES`); added `src/lib/format.ts` (`formatDate`) shared by the new view/edit-form components; consolidated the Prisma `include` shape (`itemType`/`tags`/`collections`) repeated across `getItemById`/`createItem`/`updateItem` in `src/lib/db/items.ts` into `ITEM_DETAIL_INCLUDE`. Pure refactor, no behavior change — `Sidebar.tsx` deliberately left untouched.
- Collection Create: wired up the write path behind the "New Collection" dialog (src/components/dashboard/NewCollectionDialog.tsx), which already existed in the top bar as a UI-only stub with a TODO. Follows the same pattern as item creation — new `createCollection` server action (src/actions/collections.ts, auth-checked, Zod-validated via `createCollectionSchema` in src/lib/validations/collections.ts) backed by a new `createCollection` query function (src/lib/db/collections.ts, scoped by `userId`, returns a fresh zero-stats `CollectionWithStats`); dialog shows a success/failure toast and calls `router.refresh()` so dashboard stats, the sidebar's recent collections, and the collections grid all pick up the new collection without a manual reload. Unit tests for `createCollection` in src/lib/db/collections.test.ts; the action and schema themselves are untested, matching the existing untested-thin-wrapper convention from items.ts/auth.ts and the untested-trivial-schema convention from `updateItemSchema`.
- Add Item to Collections: `CreateItemDialog.tsx` and `ItemDrawerEditForm.tsx` both gained a "Collections" field using a new shared `CollectionSelect` component (toggleable chip list, same visual pattern as the existing type-selector row) fed by a new `getAllCollections(userId)` query (src/lib/db/collections.ts, all of a user's collections sorted by name — distinct from the existing recent/favorite subsets); `createItemSchema`/`updateItemSchema` (src/lib/validations/items.ts) gained a required `collectionIds: z.array(z.string())` field; `createItem`/`updateItem` (src/lib/db/items.ts) write `ItemCollection` join rows via a new `resolveOwnedCollectionIds` helper that filters the submitted ids down to collections the user actually owns before writing (prevents linking an item to another user's collection), then `create` (createItem) or `deleteMany` + `create` (updateItem, since `ItemCollection` is an explicit join model rather than an implicit m2m like `Tag`) on the relation; `collections` threaded from the `(app)` layout's existing collection fetches down through `TopBar`/`ItemDrawer`, and independently fetched in `/items/[type]/page.tsx` for its own dialog instance. Deliberately out of scope: collection detail/browse pages, untouched. Unit tests added for the ownership-filtering behavior in both `createItem` and `updateItem` (src/lib/db/items.test.ts).
- Collections List & Detail Pages: `/collections` (src/app/(app)/collections/page.tsx) lists every collection via a new `getAllCollectionsWithStats` (src/lib/db/collections.ts, same `toCollectionWithStats` shape as `getRecentCollections`/`getFavoriteCollections` but unfiltered, name-sorted), reusing `CollectionCard`; `/collections/[id]` (src/app/(app)/collections/[id]/page.tsx) shows that collection's items via new ownership-checked `getCollectionById` and `getItemsByCollection` (src/lib/db/items.ts), splitting items into a grid (`ItemCard`/`ImageCard` by type) and a `file`-type divide-y list (`FileListRow`), same per-type-swap pattern as `/items/[type]/page.tsx`. The sidebar's "View all collections" link and every `CollectionCard` link already pointed at these routes — no link changes needed, they just had nowhere to land. Added `/collections/:path*` to `proxy.ts`'s protected matcher (previously missing, unlike `/items/:path*`). No new tests: all three new query functions are plain ownership-scoped Prisma passthroughs with no independent logic, matching the existing untested-read convention (`getItemsByType`, `getRecentCollections`, etc.). Browser-verified end-to-end (list → detail → item create into collection → drawer → delete).
- Collection Edit/Delete/Favorite Actions: `/collections/[id]` header gains Edit/Delete/Favorite via new `CollectionPageActions.tsx` (client) — Edit opens `EditCollectionDialog.tsx` (name/description, mirrors `NewCollectionDialog.tsx`), Delete opens `DeleteCollectionDialog.tsx` then redirects to `/collections` on success, Favorite renders inert (no mutation yet, matching the existing unwired Favorite/Pin pattern in `ItemDrawerActions`). `CollectionCard.tsx` converted from a whole-card `<Link>` to a client component: the card body navigates via `router.push` on click, and a 3-dot dropdown (Edit/Delete/Favorite, same two dialogs) sits in the header with a stopPropagation wrapper so it doesn't also trigger navigation. Backed by new ownership-checked `updateCollection`/`deleteCollection` query functions (src/lib/db/collections.ts, same `findFirst`-then-mutate pattern as `updateItem`/`deleteItem`) and server actions (src/actions/collections.ts); `deleteCollection` only removes the `Collection` row — `ItemCollection` join rows cascade via the existing schema relation, `Item` rows are untouched. `updateCollectionSchema` added to src/lib/validations/collections.ts. `EditCollectionDialog` resets its form fields by remounting via a `key` prop tied to open/close transitions (React's documented "reset state with a key" pattern) rather than a `useEffect`-based sync, which the project's react-hooks lint rule flags. Fixed during iteration: the delete-from-detail-page flow wasn't redirecting because the confirmation dialog was never closed and a redundant `router.refresh()` was racing the `router.push()` — now just closes the dialog and pushes. No new unit tests: matches the existing untested-thin-wrapper convention, though `updateCollection`/`deleteCollection`'s ownership-scoping logic is a candidate for tests later (parallel to `updateItem`/`deleteItem` coverage in items.test.ts).
- Global Search / Command Palette: Cmd+K / Ctrl+K opens a `cmdk`-based command palette (shadcn's `Command` component, newly installed via `npx shadcn add command` — pulled in `command.tsx` and its `input-group.tsx` dependency, plus the `cmdk` package) grouping results into "Items" and "Collections" via `CommandGroup`, with client-side fuzzy filtering handled entirely by cmdk's built-in scoring (no server round-trips, arrow-key nav and Enter-to-select come free from cmdk too); selecting an item opens the existing item drawer (`useItemDrawer().openItem`), selecting a collection navigates to `/collections/[id]`. New `getSearchableItems(userId)` (src/lib/db/items.ts) feeds the Items group (id/title/type/content-preview, with a `content ?? description ?? url` fallback truncated to 140 chars — unit tested in items.test.ts); the Collections group reuses the existing `getAllCollectionsWithStats`. Both are prefetched once in the `(app)` layout alongside the other existing fetches, so results self-refresh via the same `router.refresh()` calls the rest of the app already relies on. New `GlobalSearchProvider`/`useGlobalSearch` context (src/components/dashboard/global-search-context.tsx, same shape as `sidebar-context`/`item-drawer-context`) holds the palette's open state; `TopBar`'s search `<Input>` became a button that opens the palette and shows a `⌘K` hint. Bundled fixes to the newly-generated `command.tsx` (both were breaking every use of the palette, not just this feature): (1) `CommandDialog` wasn't wrapping `children` in the `<Command>` root primitive, so `CommandInput`/`CommandList` had no cmdk store to subscribe to (`Cannot read properties of undefined (reading 'subscribe')`); (2) a leftover `top-1/3` override on `DialogContent` fought the shared `DialogViewport` flex-spacer centering used by every other dialog in the app (`relative` positioning meant `top-1/3` shifted the popup down an extra 33% of its own height below center) — removed so it centers like every other dialog.
- Pagination: `/items/[type]`, `/collections/[id]`, and `/collections` (added past the original two-page spec, since it's the only page that actually lists collections and previously fetched all of them unbounded) now paginate via a new shared `Pagination` component (src/components/dashboard/Pagination.tsx — numbered page links + prev/next as `Button`+`Link`, prev/next render `disabled` instead of a link when unavailable, renders nothing for a single page) and `src/lib/pagination.ts` (`paginationSkip`/`toPaginatedResult` helpers, `PaginatedResult<T>` type). New `src/lib/constants.ts` holds `ITEMS_PER_PAGE`/`COLLECTIONS_PER_PAGE` (21) and `DASHBOARD_COLLECTIONS_LIMIT`/`DASHBOARD_RECENT_ITEMS_LIMIT` (6/10, replacing the previous per-file magic-number defaults on `getRecentCollections`/`getRecentItems`). `getItemsByType`/`getItemsByCollection` (src/lib/db/items.ts) and `getAllCollectionsWithStats` (src/lib/db/collections.ts) now take a `page` arg and use Prisma `skip`/`take` + a parallel `count` query instead of fetching everything. Split `getAllCollectionsWithStats` into that (paginated) and a new `getSearchableCollections` (unpaginated, mirrors the existing `getSearchableItems` pattern) since the command-palette search needs to see every collection, not just one page — the `(app)` layout was switched to the new function so global search wasn't silently broken past 21 collections. Unit tests added for the pagination math (src/lib/pagination.test.ts) and the paginated query functions (items.test.ts, collections.test.ts).
- Set Password for OAuth-only Accounts: `/profile`'s Account card now renders `SetPasswordDialog` instead of `ChangePasswordDialog` when `!user.hasPassword` (i.e. GitHub-only accounts) — new dialog asks only for new password + confirm (no current-password field, since none exists), backed by a new `setPassword` server action (src/actions/profile.ts, auth-checked, rejects if a `passwordHash` already exists, hashes via bcrypt) and `setPasswordSchema` (src/lib/validations/auth.ts, sibling to `changePasswordSchema` minus the current-password field). Bundled in the same branch: small `lucide-react` icons added to the profile action buttons (`Key` on Change/Set password, `Trash2` on Delete account), matching the existing icon+label `Button` pattern from `ItemDrawerActions.tsx`. No new tests, matching the existing untested-thin-wrapper convention for profile actions.
- Settings Page: new protected `/settings` page (src/app/settings/page.tsx, same `auth()`/`redirect` + `Card` layout pattern as `/profile`, added to `proxy.ts`'s matcher) reachable via a new "Settings" link in the sidebar's user dropdown (`UserMenu.tsx`, above the existing "Sign out" item). The Account card (password dialog — `ChangePasswordDialog`/`SetPasswordDialog` swapped on `user.hasPassword` — and `DeleteAccountDialog`) moved from `/profile` to `/settings`; `/profile` now only shows the profile info card and `ProfileStats`. All three dialogs were already route-agnostic (no hardcoded `/profile` references), so the move was a straight relocation with no dialog changes needed.
- Editor Preferences Settings: new "Editor Preferences" card on `/settings` (`EditorPreferencesForm.tsx`, using new `Select`/`Switch` shadcn primitives — didn't exist before) with font size/tab size/theme dropdowns and word wrap/minimap toggles, backed by a new `editorPreferences` JSON column on `User` (Prisma migration `20260805131254_add_editor_preferences`, nullable `Json?`) via `getEditorPreferences`/`updateEditorPreferences` (src/lib/db/settings.ts) and an auth-checked `updateEditorPreferences` server action (src/actions/settings.ts, Zod-validated via `editorPreferencesSchema` in src/lib/validations/settings.ts); defaults/merge logic and the `EditorPreferences` shape live in `src/lib/editor-preferences.ts` (`toEditorPreferences` merges stored JSON over `DEFAULT_EDITOR_PREFERENCES` so partial/legacy records don't break). New `EditorPreferencesContext` (src/components/dashboard/editor-preferences-context.tsx) provides the current preferences app-wide via the `(app)` layout; `CodeEditor.tsx` reads from it to drive Monaco's `fontSize`/`tabSize`/`wordWrap`/`minimap`/theme instead of its previous hardcoded `synapse-dark`-only setup. Each field auto-saves on change (no save button) and shows a success/error toast. Unit tests for `getEditorPreferences`/`updateEditorPreferences` in src/lib/db/settings.test.ts (defaults, merged preferences, missing-user fallback, scoped persistence); the action/schema are untested, matching the existing untested-thin-wrapper convention. Bug caught by `npm run build` before commit (not previously surfaced since `npm test`/dev don't type-check Prisma's generated JSON input types): `updateEditorPreferences` (src/lib/db/settings.ts) passed the typed `EditorPreferences` object directly as Prisma's `Json` field value, which fails because `InputJsonObject` requires a string index signature that a concrete interface doesn't have — fixed with an `as unknown as Prisma.InputJsonValue` cast (`Prisma` imported from `@/generated/prisma/client`, matching the existing pattern in `src/lib/db/collections.ts`).
- Favorites Page: new protected `/favorites` route (src/app/(app)/favorites/page.tsx, added to proxy.ts's matcher) showing all of a user's favorited items and collections in a dense, monospace, terminal-style list (`FavoritesList.tsx` — divide-y rows, no cards) split into "Items"/"Collections" sections with counts, sorted by `updatedAt` desc; clicking an item opens the existing `ItemDrawer`, clicking a collection navigates to `/collections/[id]`; empty states per section and overall. New `getFavoriteItems` query (src/lib/db/items.ts) alongside the existing `getFavoriteCollections`. Star icon link added to `TopBar.tsx`. Since the favorites-spec only covered the display page and nothing in the app previously set `isFavorite` (confirmed with the user before starting), this branch also wired up favoriting itself: new `toggleItemFavorite`/`toggleCollectionFavorite` query functions and server actions (ownership-checked, flip-and-return pattern matching `updateItem`/`updateCollection`), wired into the previously-inert Favorite buttons in `ItemDrawerActions`/`ItemDrawer` and `CollectionPageActions`, plus the same inert "Favorite" dropdown item in `CollectionCard.tsx` (bundled fix, same root cause). `CollectionWithStats` (src/lib/db/collections.ts) gained an `updatedAt` field — needed for the favorites list's "date added" column; there's no dedicated `favoritedAt` column, so `updatedAt` doubles as the "most recently favorited" proxy, matching the sort `getFavoriteCollections` already used. Unit tests added for `getFavoriteItems`/`toggleItemFavorite` (items.test.ts) and `toggleCollectionFavorite` (collections.test.ts). Known pre-existing issue (not introduced by this branch, left alone as out of scope): `npm run lint` reports a `react-hooks/set-state-in-effect` error in `ItemDrawer.tsx`'s item-fetch effect (`setItem(null)` called synchronously in the effect body) — confirmed via diff to predate this feature.
- Favorite Toggle Buttons on Cards: `ItemCard`, `ImageCard`, and `FileListRow` (previously showing only a static star indicator for `isFavorite`) each gained a clickable ghost icon-button that calls the existing `toggleItemFavorite` action with optimistic local state (flip immediately, revert + toast on failure) and `stopPropagation` so it doesn't also open the item drawer; `ImageCard`'s root element changed from a native `<button>` to `div[role=button]` since a native button can't contain the nested toggle button (same pattern `FileListRow` already used for its Download link). `CollectionCard.tsx`'s static star became a directly clickable toggle on the card header (calling the existing `toggleCollectionFavorite`), and the now-redundant "Favorite" entry was removed from its 3-dot dropdown menu. Bundled in the same branch: success toasts ("Added to favorites"/"Removed from favorites") added to every favorite toggle site, including the drawer (`ItemDrawer.tsx`) and collection detail page (`CollectionPageActions.tsx`), which previously only toasted on failure. No new server actions, schema, or tests — pure UI wiring onto the `toggleItemFavorite`/`toggleCollectionFavorite` actions built during the earlier Favorites Page feature.
- Favorites Page Sorting: `FavoritesList.tsx` (src/components/dashboard/FavoritesList.tsx) gained independent per-section sort dropdowns — Name/Date/Type for Items, Name/Date for Collections (Type omitted there since every collection's type label is just "collection") — using the existing shadcn `Select` primitive, styled to match the page's monospace terminal look; sorting is pure client-side via `useMemo` (no server round-trip), Date sorts by `updatedAt` desc (the existing "favorited-at" proxy), Name/Type sort alphabetically via `localeCompare`. Passed `items={labels}` directly to the Base UI `Select` root so the trigger resolves the capitalized label ("Date"/"Name"/"Type") on first paint instead of briefly falling back to the raw lowercase sort key before the popup's items register. No new server actions, schema, or tests — pure client-side UI logic on data the page already fetches.
- Pinned Items: the previously inert Pin button in `ItemDrawerActions.tsx` is now wired up, following the same pattern as the earlier Favorite feature — new `toggleItemPin` query fn (src/lib/db/items.ts) and server action (src/actions/items.ts), mirroring `toggleItemFavorite` exactly (flip-and-return, ownership-checked); `ItemDrawer.tsx` gained a `handleTogglePin` handler with a success/failure toast and `router.refresh()`. `getItemsByType`/`getItemsByCollection` (src/lib/db/items.ts) now `orderBy: [{ isPinned: "desc" }, { updatedAt: "desc" }]` so pinned items float to the top of `/items/[type]` and `/collections/[id]` listings; the dashboard's pinned section already read `isPinned` via the existing `getPinnedItems`, so it needed no changes. Items only, per spec — `ItemCard`'s pin icon stays a static indicator, not made clickable. Unit tests added for `toggleItemPin` (both flip directions, not-found case) and the updated `orderBy` in `getItemsByType`/`getItemsByCollection` (src/lib/db/items.test.ts). Known pre-existing issue (not introduced by this branch, left alone as out of scope): `npm run lint` still reports the `react-hooks/set-state-in-effect` error in `ItemDrawer.tsx`'s item-fetch effect, first flagged during the Favorites Page feature.
- Homepage Mockup: standalone static marketing prototype at `prototypes/homepage/` (`index.html`/`styles.css`/`script.js`, plain HTML/CSS/JS — not part of the Next.js app, per spec in context/features/homepage-mockup-spec.md), built from user-provided spec via `/feature load`. Hero "chaos → order" visual: 8 hand-crafted SVG icons (Notion, GitHub, Slack, VS Code, browser tabs, terminal, text file, bookmark) animated with a custom requestAnimationFrame physics loop (wall-bounce, mouse-repel, rotation/scale pulsing) inside `.chaos-container`, a CSS-pulsed arrow, and a `.dashboard-preview` mini-mockup with a search bar + add button and 6 item cards (type-colored icon + skeleton title/subtitle lines, added after user feedback that the initial empty-bordered-box version looked too sparse). Features/AI/Pricing/CTA/Footer sections match spec; pricing has a working monthly/yearly (`$8` ↔ `$6`, "Save 25%") toggle via `data-monthly`/`data-yearly` attributes. `IntersectionObserver`-driven scroll fade-ins and scroll-based navbar opacity implemented in `script.js`. Mobile: chaos/arrow/dashboard stack vertically with the arrow rotated 90°, and a previously-decorative-only hamburger button was wired up into a working nav dropdown (`.navbar.nav-open`) since a dead toggle would've been a worse regression than not adding one. Browser-verified with Playwright at desktop/mobile viewports (including scripted incremental-scroll to trigger the fade-in observer for full-page screenshots, since a single jump-to-bottom scroll doesn't fire it). Added `.playwright-mcp` to `.gitignore` (verification-tool debug output, wasn't previously excluded). Explicitly declined in this pass: swapping the main Next.js app's global font (currently Geist Mono everywhere) to match the homepage's system-sans stack — user deferred it for later.
- Real Homepage (from prototype): `src/app/page.tsx` now renders the real marketing homepage instead of an unconditional redirect — `auth()` check redirects to `/dashboard` only for signed-in visitors (same pattern as `(app)/layout.tsx`, condition inverted), signed-out visitors see the page. Ported `prototypes/homepage/` into 11 new components under `src/components/homepage/` (`Navbar`, `HeroSection`, `ChaosVisual`, `FeaturesSection`, `AISection`, `PricingSection`, `PricingToggle`, `CTASection`, `Footer`, `FadeIn`, `GradientButton`) using Tailwind v4 utilities/arbitrary values instead of the mockup's `styles.css` — kept the mockup's indigo/pink/amber brand-gradient identity via arbitrary hex (`bg-[#6366f1]` etc., same pattern as `CodeEditor.tsx`) rather than the dashboard's neutral `--primary` token, while structural surfaces (backgrounds/borders/muted text) use the app's real dark-mode tokens (`bg-background`/`bg-card`/`border-border`/`text-muted-foreground`, html already carries the `dark` class globally). `type-colors.ts` hardcodes project-overview.md §6's item-type colors/icons (public/unauthenticated page, no `ItemType` DB query) shared by the features grid and hero dashboard-preview mini-cards. `ChaosVisual`'s icon-physics loop (drift/wall-bounce/mouse-repel/rotate-scale pulse) is a near-verbatim port of the mockup's `script.js` into a `requestAnimationFrame` effect driving refs directly (no React state per frame); a new `arrow-pulse` keyframe/utility added to `globals.css` for the hero's transform-arrow. All CTA/nav buttons compose the shared `Button` primitive via `render={<Link/>}` (matching the codebase's existing auth-pages/`TopBar`/`Pagination` convention) instead of hand-rolled links — the repeated indigo-gradient variant lives in one new `GradientButton` wrapper rather than being duplicated across the ~9 call sites. Bug caught during `/feature review` (before it ever shipped): `Navbar`'s scroll-opacity effect read `window.scrollY`, which never changes because the root layout keeps `<body>` `overflow-hidden`/`h-full` for the dashboard's fixed app-shell — this page scrolls its own wrapper `<div>` instead (`id="homepage-scroll"`), so the listener now reads `scrollEl.scrollTop`. No new server actions/utilities/schemas — everything is presentational, so no new unit tests, matching the project's "server actions and utilities only" testing convention; existing 60-test suite unaffected. Browser verification was skipped per standing user preference against Playwright screenshots; user to eyeball `/` signed-out before considering this fully done.
- Recent Page: fixed the dead "Recent" sidebar nav link (`/recent` had no matching route) found during a UI review, by building the "Recently used" feature listed in project-overview.md section 3E but never implemented. New protected `/recent` route (src/app/(app)/recent/page.tsx, added to proxy.ts's matcher) mirrors `/favorites`' layout — a dense monospace list split into Items/Collections sections, no sort controls since "recent" is inherently date-sorted. Reuses existing `getRecentItems`/`getRecentCollections` (src/lib/db/items.ts, src/lib/db/collections.ts) via a new `RecentList.tsx` component, called with a new `RECENT_PAGE_LIMIT` (50, src/lib/constants.ts) instead of the small dashboard-preview limits. Clicking an item opens the existing `ItemDrawer`, clicking a collection navigates to `/collections/[id]`, same as Favorites. A second bug flagged in the same review (mobile sidebar drawer appearing non-functional) did not reproduce across three retests — false positive from test flakiness in that session, no fix made.
- UI Review Fixes (Medium/Low): six issues from a Playwright-based UI review fixed. (1) `ItemCard.tsx` title truncation — titles hard-clipped mid-word with no ellipsis; root cause was two-layered: the title `<span>` needed `min-w-0 flex-1` for its `truncate` to engage inside the `CardTitle` flex row, AND `CardTitle` itself needed `min-w-0` since it's a grid item inside `CardHeader` (`display:grid`) which defaults to `min-width:auto`, letting it grow past the card's width where `Card`'s `overflow-hidden` was hard-clipping pixels without ever giving the ellipsis a constrained box to apply to. (2) Homepage `AISection.tsx`'s code preview overflowed on mobile and forced the whole page horizontally scrollable, despite the `<pre>` already having `overflow-x-auto` — same root cause class, the `FadeIn` grid-item wrapper around it needed `min-w-0` so the grid column could actually shrink to viewport width and let the pre's own scroll activate. (3) Dashboard `TopBar.tsx` didn't adapt to mobile — search bar, "New Collection", and "New item" now collapse to icon-only below `sm:` (label text `hidden sm:inline`), fixing the wrapping/clipping seen at 390px. (4) `ChaosVisual.tsx`'s hero icons had zero separation force between each other (only mouse-repel and wall-bounce existed), so they could drift through and visually overlap; added an O(n²) pairwise separation step (8 icons, trivial cost) run each tick before the wall-bounce clamp. (5) `Footer.tsx`'s non-functional "About/Blog/Privacy/Terms" text now reads as clearly inert (`text-muted-foreground/40`, `cursor-default`, `title="Coming soon"`) instead of looking like unstyled dead links. (6) `TopBar.tsx`'s Notifications bell is now `disabled` with a "Notifications coming soon" tooltip instead of being a silently-inert always-clickable-looking control. Two other findings from the same review — a mobile sidebar drawer that appeared non-functional, and a mobile nav dropdown with a translucent background — did not reproduce across repeated clean retests (fresh navigations, zero-delay screenshots, computed-style checks all showed correct behavior) and were left alone as false positives rather than "fixed" without a real bug.
- Homepage Nav on Auth Pages + Dashboard Logo Mark: `/sign-in` and `/register` (src/app/sign-in/page.tsx, src/app/register/page.tsx) now render the homepage's `Navbar` (unchanged, including its own Sign In/Get Started CTAs) above the existing `AuthCard`, with a `h-18` spacer div to offset the fixed nav — matches the homepage's fixed-nav + scrollable-wrapper pattern rather than modifying the shared `AuthCard` component (which is also used by `/forgot-password`, `/reset-password`, `/verify-email`, left untouched). Dashboard nav's logo mark (`src/components/dashboard/Logo.tsx`, `MobileSidebar.tsx`) swapped from a circle-badge-with-Plus-icon to the homepage's "◆" diamond glyph (`text-[#6366f1]`) for visual consistency between the marketing site and the app; unused `Plus` imports removed from both files. A follow-up request to also make the dashboard logo clickable (linking to `/`) was implemented then explicitly rolled back per user request before completion — logo mark stays static. No new server actions/utilities/schemas — presentational only, no new tests.
- Stripe Integration Phase 1 (Core Infrastructure): `src/lib/stripe.ts` Stripe SDK singleton (`stripe@22.4.0`, `apiVersion: "2026-07-29.dahlia"` — fetched via context7, not guessed) + `STRIPE_PRICES`; `src/lib/db/billing.ts` (`getUserBilling`/`getUserByStripeCustomerId`/`setUserSubscription`), same shape as `db/profile.ts`. Fixed a pre-existing bug where `isPro` never entered the JWT/session: `src/auth.ts` gained a `jwt` callback that unconditionally re-reads `isPro` from the DB by `token.sub` on every token refresh (not just sign-in, since nothing in the app calls `update()`), copied into the session via the existing `session` callback; `isPro: boolean` added to both the `Session.user` and `JWT` augmentations (`src/types/next-auth.d.ts`). `src/components/dashboard/Sidebar.tsx` (`SidebarUser` type gained `isPro`) now reads `user.isPro` instead of `currentUser.isPro` from `mock-data` for both the file/image lock badges and the "Upgrade to Pro" card; `(app)/layout.tsx` passes `isPro: session.user.isPro` through. `MobileSidebar.tsx` needed no changes — it already renders the same shared `SidebarContent` function, so the fix covers mobile automatically (the plan doc's note that mobile lacked any lock/upgrade UI was stale). New standalone `src/lib/usage-limits.ts` (`FREE_ITEM_LIMIT = 50`, `FREE_COLLECTION_LIMIT = 3`, `PRO_ONLY_TYPE_NAMES`, `isProOnlyType`/`canCreateItem`/`canCreateCollection`) — deliberately framework-free (no Prisma/DB/session imports) so it's unit-testable and reusable by Phase 2's gating; full boundary-case coverage in `src/lib/usage-limits.test.ts` (12 new tests). Explicitly out of scope, deferred to Phase 2: checkout, webhooks, billing UI, and wiring `usage-limits.ts` into `createItem`/`createCollection`. Manually verified via Neon MCP on the `development` branch: flipped a real user's `isPro` to `true` and back via `run_sql`, confirming the Sidebar/MobileSidebar update on a plain page reload with no sign-out/sign-in needed.
- Stripe Integration Phase 2 (Integration & UI): `src/app/api/webhooks/stripe/route.ts` handles `checkout.session.completed`/`customer.subscription.updated`/`.deleted`, verified via `stripe-signature` (400 + no DB write on missing/invalid signature); reads `current_period_end`/`price` off `subscription.items.data[0]` rather than the top-level `Subscription` object, since Stripe moved those fields to subscription items in recent API versions (confirmed via Stripe's docs, not assumed). `src/actions/billing.ts` (`createCheckoutSession`/`createPortalSession`) creates a Stripe customer on first checkout and `redirect()`s to Stripe-hosted pages, following the existing try/catch action pattern with `redirect()` outside the catch so its internal throw propagates. New `/billing` page (protected, same shape as `/settings`) + `PlanCard.tsx` (upgrade flow with monthly/yearly toggle reusing `PricingToggle.tsx`'s pattern for free users; plan/period-end/manage-subscription for Pro users); added to `proxy.ts`'s matcher. Added optional `stripePriceId`/`stripeCurrentPeriodEnd` columns to `User` (migration `20260810140654_add_stripe_period_fields`, decided with the user before starting) so a canceled subscription stays Pro until the paid period actually ends rather than lapsing immediately; `setUserSubscription` (Phase 1's `src/lib/db/billing.ts`) extended to match. `createItem`/`createCollection` (`src/lib/db/items.ts`/`collections.ts`) now call Phase 1's `canCreateItem`/`canCreateCollection` against a live Prisma count before writing and return a `{ item/collection, error }` outcome instead of a bare nullable, so `src/actions/items.ts`/`collections.ts` can surface the specific rejection reason (limit reached vs. Pro-only type) — enforced in the query layer itself, so the gate holds even if a Server Action is called directly rather than through the UI. Unit tests added for both rejection paths and the Pro bypass in `items.test.ts`/`collections.test.ts` (existing `createItem`/`createCollection` tests updated for the new signature/return shape). Bug caught by `npm run build` before commit: the `stripe` package now throws immediately if constructed with an empty API key, and Next evaluates route modules during build-time page-data collection even for routes that are never called — so Phase 1's eager `new Stripe(...)` at module scope in `src/lib/stripe.ts` crashed the build the moment this phase's webhook/actions first imported it; fixed by deferring construction to a `getStripe()` accessor called on first real use. Live end-to-end verification (Stripe test-mode checkout, `stripe listen` webhook delivery, cancel/period-end behavior, direct-action bypass rejection) performed by the user against their own Stripe test-mode dashboard, which this session has no interactive access to.
- Demo Seed Data Trim: reduced `prisma/seed.ts`'s `COLLECTIONS` from 5 to 3 (removed Terminal Commands, Design Resources) to match the free plan's 3-collection limit. Since seeding only upserts what's in the list and doesn't delete extras, also deleted the demo user's now-orphaned 2 collections + 8 items directly on the `development` branch (confirmed via Neon MCP that none of those items were shared with other collections first), then re-ran `db:delete-non-demo-users --confirm` (removing 3 other accounts, including the session's own test account, per explicit request) and `db:seed` so `development` now has only `demo@devstash.io` with 3 collections / 10 items.
- Toast Notification Stacking Fix: bumped the toast viewport's z-index from `z-50` to `z-100` (`src/components/ui/toast.tsx`) — it shared `z-50` with `Dialog`/`Sheet` overlays, so a dialog's later-appended DOM node could paint on top and hide a toast fired while the dialog was still open (e.g. a failed collection-create error toast hidden behind the create dialog).
- Pro Gating for Files/Images + Billing Moved into Settings: free users visiting `/items/files` or `/items/images` (`src/app/(app)/items/[type]/page.tsx`) now see a lock icon and upgrade prompt instead of the item list, gated on the existing `isProOnlyType` check from `usage-limits.ts` (no extra DB query needed). Checkout `success_url`/`cancel_url` and the billing-portal `return_url` (`src/actions/billing.ts`) now point at `/settings` instead of `/billing`; `/billing` is now a bare `redirect()` to `/settings` (`src/app/billing/page.tsx`) and was removed from `proxy.ts`'s protected matcher. New `ProUpgradeToast` (`src/components/billing/ProUpgradeToast.tsx`) fires a "You're now Pro!" toast when redirected back with `?success=true` and strips the query param via `router.replace`. `/settings` itself moved from a standalone top-level route into the `(app)` route group (`src/app/(app)/settings/page.tsx`; old `src/app/settings/page.tsx` deleted) so it renders inside the real Sidebar/TopBar shell instead of a centered card layout, and its content was redesigned to match a provided mockup: `PlanCard.tsx` now shows a Free/Pro badge, item/collection usage counts (new `getUsageCounts` in `src/lib/db/billing.ts`) for free users, and two direct-purchase buttons ("Upgrade $8/mo" / "Upgrade $72/yr") instead of the old monthly/yearly toggle, plus a new `showUsage` prop (off on the Files/Images gate page, where usage counts aren't relevant); Editor Preferences (`src/components/settings/EditorPreferencesForm.tsx`) and the Account section (Password/Delete Account) restyled into labeled, divided rows. A live bug was found and root-caused during this work: after a real Stripe test-mode checkout, `isPro` stayed `false` because no `stripe listen` process was forwarding webhooks to `localhost:3000` (Stripe can't reach a local dev server directly) — started `stripe listen --forward-to localhost:3000/api/webhooks/stripe` and confirmed via Neon MCP that all forwarded events, including `checkout.session.completed`, returned `200` and `isPro` flipped to `true`. Excluded from this pass per user request: the mockup's "Data" export/import section, which doesn't exist in the app yet.
- Upgrade Page: new `/upgrade` page (`src/app/(app)/upgrade/page.tsx`) replaces the direct-to-checkout flow for free users — header's `TopBar.tsx` gained a subtle ghost "Upgrade" button (`isPro` threaded in from the `(app)` layout, previously not passed to `TopBar` at all) linking to it, visible only when `!isPro`. The page itself mirrors the homepage's `PricingToggle.tsx` look (Free/Pro cards, monthly/yearly toggle) via a new `UpgradePricingCards.tsx`, but the Pro CTA calls `createCheckoutSession` directly (same pattern as `PlanCard.tsx`) instead of linking to `/register`; Pro users visiting `/upgrade` directly are redirected to `/settings`. The Pro-only Files/Images gate on `/items/[type]/page.tsx` — previously an inline lock-icon + `PlanCard` prompt — now just `redirect()`s free users straight to `/upgrade` instead (that inline UI and its `PlanCard`/`Lock` usage were removed from the page). Also removed the unused pre-Prisma `src/lib/mock-data.ts` (483 lines, no remaining references) and fixed a stale routing-map table in `context/project-overview.md` (missing `/dashboard`, `/favorites`, `/recent`, `/profile`, `/upgrade`, auth pages; `/` mislabeled as the dashboard; nonexistent `/items/[type]/[id]` and `/search` page rows removed) during a `/cleanup` pass on the same branch.
- Language Dropdown for Snippets/Commands: replaces the free-text language `Textarea`/`Input` for snippet/command items with a `LanguageSelect` dropdown (src/components/dashboard/LanguageSelect.tsx, shadcn `Select`) in both `CreateItemDialog.tsx` and `ItemDrawerEditForm.tsx`, moved above the Content field. New `src/lib/languages.ts` holds a curated `LANGUAGE_OPTIONS` list keyed by Monaco language ids (so a selection drives `CodeEditor.tsx`'s syntax highlighting directly, no extra mapping step) plus `getLanguageLabel`/`DEFAULT_LANGUAGE`; unlisted/legacy language values already stored on existing items are still shown (capitalized via `getLanguageLabel`'s fallback) as an extra option rather than being silently dropped or reset. Monaco's `language` prop was already reactive, so highlighting updates live as the dropdown selection changes — no `CodeEditor.tsx` behavior changes needed beyond a minor prop passthrough. No new server actions/schema/tests — presentational input-control swap only, same convention as other pure-UI features.
- AI Auto-Tagging: establishes the app's first AI feature and Gemini foundation — `src/lib/ai/gemini-client.ts` (deferred `getGeminiClient()` singleton, same pattern as `stripe.ts`, plus the `AI_MODEL` constant) and a new `rateLimiters.autoTag` (20 req/hour/user, `src/lib/rate-limit.ts`); built against `gemini-3.5-flash-lite` rather than the spec's literal `gemini-2.5-flash-lite`, since research (`docs/ai-integration-plan.md`) found the latter scheduled to retire on the Gemini API Oct 16, 2026. New `generateAutoTags` server action (`src/actions/ai.ts`, auth + Pro-gated + Zod-validated via `src/lib/validations/ai.ts` + rate-limited) sends an item's title + content (truncated to 2000 chars) to Gemini via `@google/genai`'s `generateContent` with `responseMimeType: "application/json"`, parses either a bare `[...]` or `{"tags":[...]}` response shape, lowercases/dedupes, and catches `ApiError` with `status === 429` for a friendly "AI is busy" message. UI: new `TagSuggestions.tsx` exports a `useTagSuggestions` hook plus two presentational pieces — `TagSuggestButton` (Sparkles ghost button, Pro-gated to render `null` for free users) sits in the Tags label row, `TagSuggestionChips` (accept/check in blue, reject/X in red) renders below the tags `Input` — wired into both `CreateItemDialog.tsx` and `ItemDrawerEditForm.tsx`, sharing state via the hook since the button and chips needed to render in different places in the layout. `isPro` had to be newly threaded down into `CreateItemDialog` (both call sites: `TopBar.tsx` and `/items/[type]/page.tsx`) and `ItemDrawer`/`ItemDrawerEditForm` (via the `(app)` layout), neither of which received it before. Bundled: extracted `src/lib/tags.ts` (`parseTagsInput`/`appendTag`) and swapped it into the pre-existing duplicated comma-split logic in `CreateItemDialog`/`ItemDrawer`, needed for case-insensitive dedup against AI suggestions. Unit tests: `src/actions/ai.test.ts` (9 tests — auth/Pro/rate-limit/validation gating, both response shapes, empty-result and 429 paths, truncation) and `src/lib/tags.test.ts` (6 tests). Also completed and merged a previously-unmerged `feature/language-dropdown` branch (found still checked out from a prior session) before starting this feature, and deleted a stray already-pushed `docs/document-ai-auto-tagging` branch (untracked, from an unknown parallel session, docs-only content this feature's own current-feature.md update superseded).
- AI Description Summary: adds a second AI feature alongside auto-tagging — a "Suggest description" button (Sparkles, same ghost/sm styling as `TagSuggestButton`) next to the Description field in both `CreateItemDialog.tsx` and `ItemDrawerEditForm.tsx`. New `generateDescription` server action (`src/actions/ai.ts`, mirrors `generateAutoTags`'s auth + Pro-gate + Zod-validate + rate-limit + 429-handling shape exactly) takes title/content/url/fileName directly as input rather than an itemId — since nothing needs to be saved first, it just summarizes whatever's currently in the form — and builds the Gemini prompt from whichever of those three context fields is populated (content for text types, url for links, fileName for file/image, which have no content field at all); parses a `{"description": "..."}` or bare-string JSON response. New `generateDescriptionSchema` (`src/lib/validations/ai.ts`) and `rateLimiters.aiSummary` (20/hour/user, same shape as `autoTag`). New `DescriptionSuggestion.tsx` exports `useDescriptionSuggestion`/`DescriptionSuggestButton`, directly overwriting the Description field on generation (no accept/reject chips like tags — a single summary just fills the field, and the user can edit or retype over it like any other value). In the item drawer's edit form, the file/image `fileName` context comes from `item.fileName` directly (not `EditState`, since the file itself isn't editable in edit mode). Unit tests: `src/actions/ai.test.ts` gained 10 tests for `generateDescription` (auth/Pro/rate-limit/validation gating, both response shapes, url/fileName fallback context, empty-result, 429, truncation).
- AI Explain Code: a third AI feature — an "Explain" button (Sparkles icon) next to Copy in `CodeEditor.tsx`'s header, snippet/command types only, item drawer read view only (not create/edit forms, since `ItemContentField` — used by those forms — never receives the new `explain` prop). Built as a streaming Route Handler (`POST /api/ai/explain-code`) rather than a literal "server action" as the spec's literal text said — decided with the user at `/feature start`, since Server Actions in this stack can't push incremental chunks to the client (no Vercel AI SDK/RSC streaming primitive in use), matching the split `docs/ai-integration-plan.md` had already recommended when it was written for the earlier AI features. Auth + Pro-gate + rate-limit (`rateLimiters.explainCode`, 20/hour/user) + Zod validation (`explainCodeSchema`), then streams Gemini's `generateContentStream` output as raw text chunks; uses a new reasoning-tier model constant `AI_MODEL_REASONING = "gemini-3.6-flash"` (`src/lib/ai/gemini-client.ts`, alongside the existing `AI_MODEL`) rather than the spec's literal `gemini-2.5-flash`, following the same current-gen-vs-retiring reasoning `docs/ai-integration-plan.md` laid out for the other AI features. New `useExplainCode` hook (`src/components/dashboard/ExplainCode.tsx`) reads a per-session in-memory cache keyed by item id (`src/lib/ai/explain-cache.ts`, explanations aren't persisted to the DB) before falling back to `fetch` + `response.body.getReader()`, updating state as chunks arrive; `CodeEditor.tsx` gained an optional `explain` prop that renders a Code/Explain tab pair (mirroring `MarkdownEditor`'s tab styling) once generation starts, the Sparkles/Loader2 trigger button (or a Crown + "AI features require Pro subscription" tooltip for free users), and a markdown-rendered explain panel reusing the `.markdown-preview` CSS class. `isPro` threaded into `ItemDrawerView` (previously only reached the edit form). `buildExplainPrompt` extracted into a framework-free `src/lib/ai/explain-prompt.ts` (title/language/content assembly + 4000-char truncation) so it's unit-testable without mocking auth/Gemini/rate-limit, matching this repo's untested-route-handler convention (register/upload/download/webhook routes are all untested too) while still covering the one piece of real logic (3 tests in `explain-prompt.test.ts`). Bug caught and fixed during `/feature review` (before it shipped): the route kept pulling/generating chunks from Gemini even after the client disconnected (drawer closed, tab navigated away) — since the free-tier quota is shared project-wide per `docs/ai-integration-plan.md`, an abandoned stream silently draining it was a real gap, not cosmetic; fixed by checking `request.signal.aborted` inside the `for await` loop and breaking early. Also fixed in the same pass: an inline retry message for the Explain panel's `error` state, since a failed generation previously left the tab blank once the toast disappeared. Browser-verified end-to-end with Playwright (real streaming into the tab, tab switching preserving Monaco's syntax highlighting, cache reuse on reopen with no re-fetch, and the Crown/tooltip gating for free users) — the demo user's `isPro` was temporarily flipped via Neon MCP for the Pro-state test and confirmed reset back to its original `false` afterward.
- AI Prompt Optimizer: a fourth AI feature — an "Optimize" button (Sparkles icon) next to Copy in `MarkdownEditor.tsx`'s header, scoped to `prompt` items only (`note` shares the same editor but doesn't get the prop), available in the editable contexts only (`CreateItemDialog.tsx` and `ItemDrawerEditForm.tsx` via a new `optimize` prop threaded through `ItemContentField.tsx`) — not the read-only `ItemDrawerView`, since accepting a suggestion needs somewhere to write the result. New `optimizePrompt` server action (`src/actions/ai.ts`, auth + Pro-gate + Zod-validate + rate-limit shape identical to `generateAutoTags`/`generateDescription`, non-streaming per a `/feature start` decision since the output needs to be fully formed before the user can accept/reject it) plus `optimizePromptSchema` (`src/lib/validations/ai.ts`) and `rateLimiters.promptOptimize` (20/hour/user). Review UI is replace-preview-then-confirm (also decided at `/feature start`, closest existing precedent being `CodeEditor.tsx`'s code/explain tab-switch): clicking Optimize swaps the editor body to the Gemini-refined text with Cancel/"Use this" actions at the bottom via a new `usePromptOptimize` hook (`src/components/dashboard/PromptOptimizer.tsx`, `idle`/`loading`/`reviewing` states) — original content stays untouched until "Use this" is clicked. Bug caught during Playwright verification (before it shipped): the initial system instruction said "Respond with JSON only" without specifying a shape, so Gemini returned `{"title": ..., "prompt": ...}` instead of the expected `{"optimized": ...}`, silently failing every real call with "No optimized prompt generated" — fixed by making the instruction state the exact JSON shape, plus added a `"prompt"`-key fallback in the parser for resilience (regression test added). Unit tests: `src/actions/ai.test.ts` gained 10 tests for `optimizePrompt` (auth/Pro/rate-limit/validation gating including empty-content rejection, both response shapes including the fallback, empty-result, 429, truncation). Browser-verified end-to-end with Playwright in both the create dialog and drawer edit form (optimize → review → accept writes into the field, both the drawer-level and review-panel-level Cancel discard correctly, button correctly absent for `note` and disabled on empty content, Crown/tooltip gating for free users) — the demo user's `isPro` was temporarily flipped via Neon MCP for the Pro-state tests and confirmed reset back to its original `false` afterward.
- Auth UI Consistency Fixes: fixed three issues found during a Playwright-driven UI review of the auth pages — `RegisterForm.tsx` gained the "Sign in with GitHub" button/divider it was missing (present on `SignInForm` but not `RegisterForm`); the app's inconsistent brand marks (a "◆" diamond on `Navbar`/`Footer`/dashboard `Logo`/`MobileSidebar`, a circle-badge-with-`Plus` on `AuthCard`) were unified to a single `Folder` (lucide-react) icon everywhere; `Navbar`'s "Sign In"/"Get Started" CTAs are now hidden on the matching `/sign-in`/`/register` route via `usePathname()`, since they duplicated the page's own action. Follow-up in the same branch: restyled `SignInForm` to a larger, more spacious layout (bigger inputs/buttons, a clearer "Or continue with" divider) per a user-provided reference screenshot, and removed `AuthCard`'s own logo block (redundant now that `Navbar` renders one above it) — deliberately kept the "Synapse" brand name rather than the "DevStash" text shown in the reference image, since that looked like an artifact of whatever tool produced the mockup rather than an actual rename request. Also fixed a real layout bug surfaced while confirming the page fits without scrolling: `AuthCard`'s wrapper combined `min-h-full` with `flex-1` inside a flex column that also holds a fixed (out-of-flow) `Navbar` and an `h-18` spacer, so `min-h-full` resolved to 100% of the parent's height on top of the spacer's height, overflowing every `AuthCard`-based page (`/sign-in`, `/register`, `/forgot-password`, `/reset-password`, `/verify-email`) by exactly the spacer's height regardless of content size — fixed by dropping `min-h-full`. Browser-verified with Playwright at multiple viewport sizes (1440×900, 1280×720) confirming zero scroll.
- Dashboard & Sidebar Polish: iterative pass on `Sidebar.tsx` against a reference dashboard mockup — tightened then rebalanced row/section spacing (settled on `py-1.5` rows, `space-y-1` lists, `space-y-4`/`space-y-3` section gaps) so the nav fits more content without feeling cramped; "Types"/"Collections" section headers enlarged and switched from muted `text-xs` to brighter `text-sm text-foreground` so they read as real headers against the colorful type rows (caps transform tried, then explicitly reverted to normal case); sidebar sub-labels renamed "Favorite collections"/"Recent collections" → "Favorites"/"Recent"; sidebar Types list and `ProfileStats.tsx`'s type breakdown now display pluralized type names (via the existing `typeNameToSlug` helper, e.g. "Snippets" not "Snippet") — deliberately left singular everywhere a type name labels one specific item (item drawer, Favorites/Recent list tags, create-dialog type picker). `/dashboard`'s content wrapped in `mx-auto max-w-6xl` so it stays centered on wide screens instead of stretching edge-to-edge (scoped to `/dashboard` only, not yet applied to `/items/[type]` or `/collections`). New reusable `CollapsibleSection.tsx` (client component, chevron toggle, defaults open) extracted so "Recent items" on the dashboard can collapse; `PinnedItemsSection` deliberately left as-is since only Recent was in scope. App-wide themed scrollbar added in `globals.css` (`scrollbar-color`/`::-webkit-scrollbar` using the `--muted`/`--background`/`--muted-foreground` tokens) replacing the default OS scrollbar everywhere, not just the sidebar. `UserMenu`/bottom account footer explicitly untouched throughout per user request. No new server actions/schema/tests — presentational only; existing 113-test suite unaffected.
- General UI Font Switch to Inter: the app previously used Geist Mono for literally everything, including UI chrome — `--font-sans` and `--font-mono` in `globals.css` both pointed at the same Geist Mono variable. Added `Inter` via `next/font/google` (`src/app/layout.tsx`, `--font-inter` CSS variable) and repointed `--font-sans` to it, leaving `--font-mono` on Geist Mono untouched; since `html { @apply font-sans }` is the single inheritance root and every code/terminal surface already used explicit `font-mono` (CodeEditor, MarkdownEditor, ItemContentField, ItemDrawerView, TopBar, AISection, RecentList, FavoritesList), this was a 2-line CSS-token change with no component edits needed. First tried Geist Sans (same foundry/metrics as Geist Mono), then swapped to Inter per user preference — closer to the app's stated Linear/Raycast design references. No new tests — presentational only.
- Server Action Helpers Refactor (Code Scan Cleanup): scanned `src/actions/*` for duplication and extracted the boilerplate that was copy-pasted across `auth.ts`/`profile.ts`/`settings.ts`/`collections.ts`/`items.ts`/`billing.ts` (18 identical auth-guard checks, 13 identical Zod-error branches, 7 near-duplicate local `ActionResult`/`ActionError` interfaces) into a shared `ActionResult<T = void>` type (`src/types/actions.ts`) and two helpers (`src/lib/actions.ts`): `requireSession()` (throws `ActionAuthError` if unauthenticated, for use inside a try/catch) and `handleActionError()` (Zod → auth → generic fallback, with logging); `auth.ts`'s `signInWithCredentials` keeps its custom `AuthError`-branching catch block but reuses the new `zodErrorMessage()` for its one shared line. Bundled fix: `profile.ts`'s `deleteAccount` previously had no try/catch at all (a pre-existing gap vs. the project's "use try/catch in Server Actions" standard) — it now goes through the same pattern as every other action. Pure refactor, no behavior change; `ai.ts`'s three near-identical Gemini-calling actions were scanned but deliberately left untouched (larger structural change, deferred). Existing 113-test suite + `npm run build` unaffected.
- Components Refactor (Code Scan Cleanup): scanned `src/components/` for duplication and oversized files via a `code-scanner` agent pass, then addressed all 10 findings — `useItemFavorite`/`useCollectionActions` hooks (src/components/dashboard/use-item-favorite.ts, use-collection-actions.ts) replacing 5 independently copy-pasted optimistic favorite/delete-toggle implementations across `ItemCard`/`ImageCard`/`FileListRow`/`CollectionCard`/`CollectionPageActions`; `formatShortDate` added to `src/lib/format.ts` (with an `includeYear` option) replacing 5 copies in `ItemRow`/`ItemCard`/`FavoritesList`/`RecentList`/`FileListRow`; `DeleteItemDialog.tsx`/`DeleteCollectionDialog.tsx` deleted in favor of one generic `ConfirmDeleteDialog`; `EntityListSection`/`EntityRow` (src/components/dashboard/EntityListSection.tsx) merging `RecentList`/`FavoritesList`'s identical row components and section structure; shared editor chrome — `useCopyToClipboard` hook, `EDITOR_MIN_HEIGHT`/`EDITOR_MAX_HEIGHT` (`src/lib/editor-chrome.ts`), and an `AiTriggerButton` component — deduplicated out of `CodeEditor`/`MarkdownEditor`; `CreateItemDialog.tsx` cut from 322 to 175 lines via a shared `ItemFormFields` (src/components/dashboard/ItemFormFields.tsx, a `variant: "create" | "edit"` prop drives the few real UI differences — label typography, "(optional)" hints, autoFocus, required-URL — so it's also now used by `ItemDrawerEditForm.tsx`) plus a standalone `ItemTypeSelector`; `useOpenItemProps` hook for the repeated drawer-open a11y markup in the three item card/row components; `Sidebar.tsx` cut from 270 to ~130 lines via `TypesNavList`/`FavoriteCollectionsNav`/`RecentCollectionsNav`/`SidebarSectionControls`; `CollectionCardMenu` extracted from `CollectionCard.tsx` for the 3-dot dropdown only (not the Edit/Delete dialogs, which stay as siblings of the card's clickable wrapper — React portal events bubble along the component tree rather than the DOM tree, so nesting the dialogs inside the clickable area would let clicks inside them trigger card navigation). Deliberately left `ItemDrawer.tsx`'s own favorite-toggle out of the `useItemFavorite` unification since it updates a full server-returned `item` object rather than a local boolean, and unifying it risked either a real bug or an over-configured hook. Pure refactor, no behavior change; verified via `npm run lint` (2 pre-existing errors confirmed unchanged by diffing against `main`), `npm run test` (113/113 passing), and `npm run build` (clean) — no Playwright pass, per standing user preference to verify UI changes in-browser themselves.
